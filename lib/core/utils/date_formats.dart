import 'package:intl/intl.dart';

class AppDates {
  const AppDates._();

  static final short = DateFormat('dd/MM/yyyy');
  static final month = DateFormat('MMM yyyy');
  static final dayMonth = DateFormat('dd MMM');
}
